# Tool list draft

- Cursor: use multi-file context to refactor modules; demonstrate structured diffs and quick navigation.
- LangGraph: prefer explicit state + deterministic supervisor routing; stream intermediate steps.
- RAG stack: embeddings (nomic-embed-text) + vector store (Chroma) + retriever + grounded synthesis.
- Responsible AI: include explainability notes (SHAP/LIME) when regulated domains are involved.

